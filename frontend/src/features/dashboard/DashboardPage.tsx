import { Typography } from '@mui/material';

function DashboardPage() {
  return (
    <Typography
      variant="h4"
      component="h1"
      sx={{
        color: 'primary.dark',
      }}
    >
      Dashboard
    </Typography>
  );
}

export default DashboardPage;